{
  "name": "wifi-drop-tauri",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight local Wi-Fi drop app for Windows and iPhone.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0"
  }
}
